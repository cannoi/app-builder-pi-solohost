# Troubleshooting

The app could not start.
- Check Docker is running.
- Check `.env` exists.
- Read the container log in your host panel.

The app could not connect to its database.
- This starter keeps data in memory/files and does not need a separate database server.
