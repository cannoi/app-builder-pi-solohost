# Installation

1. Install Docker.
2. Copy this project to your SoloHost.
3. Copy `.env.example` to `.env`.
4. Run `docker compose up -d --build`.
5. Open `http://HOST-IP:8080`.

The app listens on `0.0.0.0:8080` inside the container.
