services:
  app:
    image: paf-app:solohost-browser-hub
    restart: unless-stopped
    labels:
      pi.ui.primary: "true"
      solohost.app: "true"
      solohost.app.id: "browser"
      solohost.app.name: "SoloHost Browser"
      solohost.app.route: "/"
      solohost.app.icon: "app"
    ports:
      - "127.0.0.1:18080:8080"
      - "127.0.0.1:18081:8081"
    environment:
      PORT: "8080"
      PROXY_PORT: "8081"
      PROXY_PUBLIC_PORT: "18081"
      SOLOHOST_DEMO_APPS: "1"
    volumes:
      - solohost-browser-data:/app/data

volumes:
  solohost-browser-data:
