/* Optional external script. Benchmark v2 runs from inline JS in index.html.
   If this file fails to load, the page still diagnoses HTML -> JS -> API -> container. */
console.log("sandbox-app-benchmark: external app.js is optional and loaded");
